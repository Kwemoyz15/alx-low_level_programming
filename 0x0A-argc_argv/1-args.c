#include <stdio.h>
#include "main.h"
/**
 * main - program that counts all arguments
 * @argc: counts arguments
 * @argv: array of arguments
 */

int main( int argc, char *argv[])
{
	printf("%d\n", argc);
	(void)argv;
	return (0);
}
